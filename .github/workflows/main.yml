name: AutoTest Search Avia

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      user_chat_id:
        description: 'ID чата для личного ответа'
        required: false
        default: ''

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1. Подготовка: создаем файлы-заглушки, чтобы cat не выдавал ошибку
      - name: Initialize logs
        run: |
          echo "NONE" > api_status.txt
          echo "EMPTY" > offers_count.txt

      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          spec: cypress/e2e/avia.cy.js
          browser: chrome
        env:
          CYPRESS_LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          CYPRESS_LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}
        continue-on-error: true

      # 2. ПАРСИНГ: Очищаем данные от лишних пробелов и символов
      - name: Parse Diagnostic Data
        id: parse_data
        if: always()
        run: |
          # Читаем и удаляем любые пробелы/переносы
          RAW_STATUS=$(cat api_status.txt | tr -d '[:space:]')
          RAW_COUNT=$(cat offers_count.txt | tr -d '[:space:]')
          
          echo "Log: Status is [$RAW_STATUS], Count is [$RAW_COUNT]"

          if [[ "$RAW_STATUS" == "200" ]]; then
            if [[ "$RAW_COUNT" =~ ^[0-9]+$ ]]; then
              if [ "$RAW_COUNT" -gt 0 ]; then
                echo "status=✅ Success" >> $GITHUB_OUTPUT
                echo "count=$RAW_COUNT" >> $GITHUB_OUTPUT
              else
                echo "status=⚪ No Tickets" >> $GITHUB_OUTPUT
                echo "count=0" >> $GITHUB_OUTPUT
              fi
            else
              echo "status=⚠️ Search finished, but results unknown" >> $GITHUB_OUTPUT
              echo "count=?" >> $GITHUB_OUTPUT
            fi
          elif [[ "$RAW_STATUS" =~ ^[4-5][0-9][0-9]$ ]]; then
            echo "status=🆘 Server Error HTTP $RAW_STATUS" >> $GITHUB_OUTPUT
            echo "count=Error" >> $GITHUB_OUTPUT
          else
            echo "status=❌ Test Failed / Crash" >> $GITHUB_OUTPUT
            echo "count=N/A" >> $GITHUB_OUTPUT
          fi

      # 3. УВЕДОМЛЕНИЕ
      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: >-
            ${{ (github.event.inputs.user_chat_id != '') && 
            github.event.inputs.user_chat_id || 
            secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }} 
          format: markdown
          message: |
            ${{ (github.event.inputs.user_chat_id != '') && '👤 **Manual Testing**' || '📢 **Hourly Monitoring**' }}
            
            ✈️ **Avia Search Result**
            **Status:** ${{ steps.parse_data.outputs.status }}
            **Offers Found:** ${{ steps.parse_data.outputs.count }}
            **Time:** ${{ github.event_name == 'schedule' && 'Hourly ⏰' || 'Manual 🔘' }}